# `pyidfm.types`

Type aliases used to disambiguate the different IDFM identifier formats —
stop points (`ArId`), stop areas (`ZdAId`), exchange areas (`ZdCId`), and
both line-ID forms (`ShortLineId`, `LineId`).

```{eval-rst}
.. automodule:: pyidfm.types
   :members:
   :undoc-members:
   :show-inheritance:
```
